export interface AcademicPeriod {
    id: number;
    name: string;
    startDate: string;
    endDate: string;
}
export interface RegisterAcademicPeriodInterface {
    name: string;
    startDate: string;
    endDate: string;
}
